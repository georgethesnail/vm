# Set your public domain where per-user subdomains will be reachable.
# Example: DOMAIN=vm.example.com  -> alice.vm.example.com
DOMAIN=localtest.me

# Admin token used to protect the manager API (change this to a strong secret).
ADMIN_TOKEN=replace-with-a-strong-secret

# Email used for Let's Encrypt (optional)
LETSENCRYPT_EMAIL=your-email@example.com
